# data — regenerated by world/build.py from a seed, never committed
data/raw/
data/derived/

# results — run traces are committed deliberately, see results/README.md
results/scratch/
*.log

# python
__pycache__/
*.py[cod]
# `uv pip install -e .` builds for real now, and drops this beside the modules
*.egg-info/
build/
dist/
.venv/
venv/
.pytest_cache/
.ruff_cache/
.coverage

# env
.env

# os
.DS_Store
